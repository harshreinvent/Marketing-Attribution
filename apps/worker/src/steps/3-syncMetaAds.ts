import type { DateRange } from "@repo/types";

export async function syncMetaAds(clientId: string, dateRange: DateRange) {
  // TODO: read integration config → fetch → validate → normalise → upsert
}
