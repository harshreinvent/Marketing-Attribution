import { PageHeader } from "@/components/common/PageHeader";

export default function AdminSyncStatusPage() {
  return (
    <div className="space-y-6">
      <PageHeader title="Sync Status" description="Monitor sync jobs across all clients" />
    </div>
  );
}
