import { prisma } from "@repo/db";
import { buildTenantWhere } from "@repo/db";
import type { TenantScope } from "@repo/db";

export const ga4Model = {
  // TODO: implement query methods using buildTenantWhere(scope)
};
